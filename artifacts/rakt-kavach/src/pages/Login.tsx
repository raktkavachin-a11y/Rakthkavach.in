import { ArrowRight, Globe2, HeartPulse, Languages, LockKeyhole, ShieldCheck, Smartphone } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { useLocation } from 'wouter';
import { useAuth, type AuthRole } from '@/context/auth';
import { languageLabels, supportedLanguages, useI18n } from '@/context/i18n';
const roles: Array<{ role: AuthRole; key: string }> = [{ role: 'donor', key: 'donor' }, { role: 'hospital', key: 'hospital' }, { role: 'laboratory', key: 'laboratory' }, { role: 'block_officer', key: 'blockOfficer' }, { role: 'district_authority', key: 'districtAuthority' }, { role: 'state_command', key: 'stateCommand' }, { role: 'national_board', key: 'nationalBoard' }, { role: 'who_command', key: 'whoCommand' }, { role: 'system_admin', key: 'systemAdmin' }];
export default function Login(): JSX.Element { const { language, setLanguage } = useI18n(); const { loginAs } = useAuth(); const [, navigate] = useLocation(); const [role, setRole] = useState<AuthRole>('donor'); const [name, setName] = useState(''); const [phone, setPhone] = useState(''); const [otp, setOtp] = useState(''); const [error, setError] = useState(''); const submit = (event: FormEvent<HTMLFormElement>): void => { event.preventDefault(); if (!/^[6-9]\d{9}$/.test(phone)) { setError('Enter a valid 10-digit Indian mobile number.'); return; } if (!/^\d{6}$/.test(otp)) { setError('Enter the six-digit OTP sent to your phone.'); return; } if (!name.trim()) { setError('Enter your name to continue.'); return; } loginAs(role, name.trim()); navigate(role === 'donor' ? '/donor' : role === 'hospital' || role === 'laboratory' ? '/hospital' : '/admin'); };
return <main className="min-h-screen bg-[#030712] p-4 text-white md:p-8"><div className="mx-auto grid min-h-[calc(100vh-4rem)] max-w-6xl items-center gap-12 lg:grid-cols-2"><section><p className="flex items-center gap-2 text-sm font-bold uppercase tracking-[.25em] text-cyan-300"><HeartPulse className="h-5 w-5" /> Rakt Kavach</p><h1 className="mt-6 text-5xl font-black leading-tight">One verified network.<br /><span className="text-cyan-300">Every life connected.</span></h1><p className="mt-5 max-w-lg text-slate-400">Secure access to India’s unified blood grid, donor wallet, emergency response, and command intelligence.</p><div className="mt-8 flex flex-wrap gap-3 text-sm text-slate-300"><span className="rounded-full border border-white/10 px-3 py-2"><ShieldCheck className="mr-2 inline h-4 w-4 text-emerald-300" /> OTP protected</span><span className="rounded-full border border-white/10 px-3 py-2"><Globe2 className="mr-2 inline h-4 w-4 text-cyan-300" /> Live network</span></div></section><form onSubmit={submit} className="rounded-3xl border border-cyan-300/20 bg-white/[.05] p-6 shadow-2xl shadow-cyan-950/30"><div className="mb-6 flex items-center justify-between"><div><h2 className="text-2xl font-bold">Secure entry</h2><p className="text-sm text-slate-400">Verify your phone to open your workspace.</p></div><Languages className="h-5 w-5 text-cyan-300" /></div><label className="mb-2 block text-sm text-slate-300">Access role<select value={role} onChange={(event) => setRole(event.target.value as AuthRole)} className="mt-2 w-full rounded-xl border border-white/10 bg-[#081426] px-3 py-3 text-white">{roles.map((item) => <option key={item.role} value={item.role}>{item.key}</option>)}</select></label><label className="mt-4 block text-sm text-slate-300">Full name<input value={name} onChange={(event) => setName(event.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3" placeholder="Your name" /></label><label className="mt-4 block text-sm text-slate-300"><Smartphone className="mr-1 inline h-4 w-4" /> Phone number<input value={phone} onChange={(event) => setPhone(event.target.value.replace(/\D/g, '').slice(0, 10))} inputMode="numeric" className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3" placeholder="9876543210" /></label><label className="mt-4 block text-sm text-slate-300"><LockKeyhole className="mr-1 inline h-4 w-4" /> Phone OTP<input value={otp} onChange={(event) => setOtp(event.target.value.replace(/\D/g, '').slice(0, 6))} inputMode="numeric" maxLength={6} className="mt-2 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-3 tracking-[.4em]" placeholder="••••••" /></label>{error && <p role="alert" className="mt-3 text-sm text-rose-300">{error}</p>}<button type="submit" className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-cyan-300 py-3 font-bold text-slate-950">Verify & enter <ArrowRight className="h-4 w-4" /></button><div className="mt-5 flex justify-center gap-2 text-xs">{supportedLanguages.map((item) => <button type="button" key={item} onClick={() => setLanguage(item)} className={language === item ? 'font-bold text-cyan-300' : 'text-slate-500'}>{languageLabels[item]}</button>)}</div></form></div></main>; }
